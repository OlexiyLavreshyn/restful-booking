# Restful Booker API Tests

API automation tests for the Restful Booker API.

The tests are implemented using:

- JavaScript
- Native Fetch API
- Node.js built-in test runner
- Node.js Assert API

## Requirements

- Node.js 18 or newer
- npm

## Installation

Clone the repository, install dependencies and start:

```bash
npm install

npm test